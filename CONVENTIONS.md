# Conventions

- All source code, identifiers, comments, API messages, and documentation are written in **English**.
- Domain-driven layering: Domain → Application → Infrastructure → Api.
- Prefer explicit types and nullable reference types (`nullable` enabled).
- Secrets belong in environment variables or secret stores — never in committed config.